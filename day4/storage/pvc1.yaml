apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myapp
  namespace: default
  labels:
    app: myapp
spec:
  storageClassName: dev1
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
